rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Unrestricted access until the end of 2026 for development
      allow read, write: if request.time < timestamp.date(2026, 12, 31);
    }
  }
}
